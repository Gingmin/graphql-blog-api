spring:
  application:
    name: blog-java-main

  datasource:
    url: jdbc:postgresql://localhost:5434/clean
    username: peuser
    password: rhtmxm

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true

  graphql:
    graphiql:
      enabled: true

logging:
  level:
    # SQL 출력(hibernate)
    org.hibernate.SQL: debug
    # 바인딩 파라미터까지 보고 싶으면 trace로
    org.hibernate.orm.jdbc.bind: trace
    # (선택) 트랜잭션 로그
    org.springframework.transaction: info

app:
  jwt:
    # DEV ONLY. 운영에서는 환경변수/시크릿 매니저로 주입하세요.
    secret: "dev-secret-change-me"
    issuer: "blog-java-main"
    ttl-seconds: 3600
