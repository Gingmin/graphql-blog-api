type Query {
  users: [User!]!
  user(id: ID!): User
}

type Mutation {
  createUser(username: String!, email: String!): User!
}

type User {
  id: ID!
  username: String!
  email: String!
  posts: [Post]
  comments: [Comment]
  followers: [User]
  following: [User]
}
