type Tag {
  id: ID!
  name: String!
  createdAt: String!
  posts: [Post!]
}

extend type Query {
  tags: [Tag!]!
  tag(id: ID!): Tag
}

extend type Mutation {
  createTag(name: String!): Tag!
  deleteTag(id: ID!): Boolean!
}
